# USER-CONFIGURABLE-MEDICINE-REMINDER-SYSTEM
The User Configurable Medicine Reminder System is designed to remind users to take medicines at scheduled times. The system continuously monitors RTC time and compares it with user-configured medicine schedules. When the configured time is reached, the LCD displays a reminder message and the buzzer generates an alert.
